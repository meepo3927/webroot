import Root from 'root';

const mutations = {
    test: function (state, payload) {
    }
}
const store = new Root.Vuex.Store({
    mutations,
    state: {},
    getters: {}
});

export default store;